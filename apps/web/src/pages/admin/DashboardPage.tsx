/**
 * Painel: total de cadastros e cadastros por cor. A tela real chega em
 * `feat/web-admin-dashboard`, sobre `GET /api/admin/summary`.
 *
 * O mockup titula esta tela com o próprio número ("143 pessoas cadastradas").
 * Como ele vem da API, a casca usa um rótulo neutro em vez de inventar um.
 */
export function DashboardPage() {
  return (
    <section>
      <h1 className="text-3xl">Painel</h1>
    </section>
  );
}
