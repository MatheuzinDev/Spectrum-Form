/**
 * Formulário público de cadastro. A tela real chega em `feat/web-client-form`,
 * com o schema de `@repo/shared`, a máscara de CPF e o `409` mapeado no campo.
 */
export function ClientFormPage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-16">
      <h1 className="text-3xl">Seu cadastro, uma vez só.</h1>
    </main>
  );
}
